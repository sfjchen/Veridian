name: Validate

on:
  pull_request:
    branches: [main]

jobs:
  config-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check config_schema.py sync
        run: bash scripts/check_config_schema_sync.sh
