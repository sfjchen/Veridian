# feat(teacher): Wave 2 — Veridian tokens on all teacher screens

## Summary
Single PR for **Wave 2 Teacher** from the [World-class UI full redesign](.cursor/plans/world-class_ui_full_redesign_1bb505e0.plan.md): auth, dashboard, navigation, create-assignment, and remaining teacher screens use only design tokens (palette, typography, spacing, radius, motion) and primitives.

## Scope
- **teacher-auth-tokens**: LoginScreen / SignupScreen — already token-only; no code change.
- **teacher-dashboard-empty**: DashboardScreen — list fade uses `motion.normal`; optional 50ms stagger per card (`StaggeredCard`); empty icon uses `typography.h1`; EmptyState + one CTA; Card `borderTopColor` primary.
- **teacher-nav-tokens**: `navigation/index.tsx` + InvalidRoleScreen — header/loading/invalid-role use palette, typography, spacing, radius.
- **teacher-create-assignment**: CreateAssignmentScreen — already on tokens; no change.
- **teacher-other-screens**: ClassroomScreen (empty icon text), AssignmentScreen (config/preview/try-student/problem styles), StudentMistakeDetailScreen (full token pass), CorpusUploadScreen (already on tokens).

## Files changed
- `teacher/frontend/src/navigation/index.tsx`
- `teacher/frontend/src/screens/teacher/DashboardScreen.tsx`
- `teacher/frontend/src/screens/teacher/ClassroomScreen.tsx`
- `teacher/frontend/src/screens/teacher/AssignmentScreen.tsx`
- `teacher/frontend/src/screens/teacher/StudentMistakeDetailScreen.tsx`

## How to open the PR
```bash
git push -u origin feat/wave2-teacher-screens
```
Then create a PR against `main` (or against `feat/wave1-foundation` if Wave 1 isn’t merged yet) and paste this description. For review, comment: **@codex @claude review**
