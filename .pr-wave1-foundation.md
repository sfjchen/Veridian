# feat: Wave 1 foundation — Veridian tokens, motion, docs, student constants

## Summary

Unified design token foundation (World-class UI plan, Wave 1): motion token, docs, and student app adopting Veridian palette and constants.

## Changes

- **tokens-motion**: `motion.slow` (400ms) in teacher constants; CLAUDE.md motion row updated to document fast/normal/slow.
- **tokens-docs**: CLAUDE.md Design tokens section — clarified where tokens live; student frontend now documented as using same Veridian values in `student/frontend/constants/`.
- **student-tokens**: Replaced `student/frontend/constants/palette.ts` with Veridian palette (primary #16A34A, surface, card, text, radius, elevation). Added `typography.ts`, `spacing.ts`, `motion.ts`, `theme.ts` so student has full token set. Kept backward-compat aliases (`errorText`, `rowPressed`, `inkStroke`, `radius.thumb`) for existing imports.

## Checklist

- [x] No raw hex in new/updated token files
- [x] Student palette matches teacher Veridian; existing `@/constants/palette` imports unchanged
- [x] Running docs (CLAUDE.md) updated

## Follow-up

Wave 2: teacher auth/dashboard/nav and student sign-in/sign-up/classes/assignments screens can consume `typography`/`spacing`/`theme` and load DM Sans in student app where needed.
